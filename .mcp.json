{
  "mcpServers": {
    "hex-cortex": {
      "command": "python",
      "args": [
        "-m",
        "hex_cortex.memory.cortex_operational_stdio"
      ],
      "env": {
        "PYTHONPATH": "${CLAUDE_PROJECT_DIR:-.}/src"
      }
    }
  }
}
